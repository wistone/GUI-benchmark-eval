# External Assets

This directory is for local external assets such as cloned benchmark repos, VM images, caches, and downloaded artifacts.

The contents are ignored by Git. Keep only this README tracked.

