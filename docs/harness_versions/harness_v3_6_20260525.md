# Harness V3.6 Prompt

Date: 2026-05-25

Purpose: keep the V3.5 rules that helped download sets, exact Impress color, Chrome setting, and Writer subscript, while fixing the main V3.5 execution failure: repeated long terminal typing.

Key changes from V3.5:
- Long text entry into the VM GUI should use clipboard paste via `pyperclip.copy(...)` plus `pyautogui.hotkey('ctrl','v')`, instead of typing hundreds of characters with `pyautogui.write`.
- If a long command/script does not execute, detect whether Terminal is at a shell prompt, Python prompt, or unfinished here-doc/quote before retrying.
- After two failed long-script entry attempts, abandon that script route and choose a simpler route or FAIL.
- Repetitive form/PDF generation tasks are limited to one concise spreadsheet inspection and one concise template inspection before producing outputs.

```text
Use pyautogui for GUI control. For long commands/config/scripts, paste text into the VM GUI with clipboard paste instead of typing it character by character. Clipboard paste is allowed only to enter text into the VM GUI, not to inspect host files or bypass the desktop/evaluator.

Do not repeat failed long command entry. If a command appears not to execute, identify the prompt state, cancel back to shell once, and retry using a shorter clipboard-pasted command. After two failed attempts, switch route or FAIL.

Preserve V3.5's useful rules:
- Save and verify Office artifacts.
- Use exact colors for Impress.
- Use early Terminal enumeration for repeated download sets.
- Use exact source/feature checks before unavailable/install FAIL.
```
