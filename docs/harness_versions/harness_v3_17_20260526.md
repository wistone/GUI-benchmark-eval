# Harness V3.17 - Preserve Repair Micro-Iteration

Date: 2026-05-26

## Goal

Keep the useful V3.16 signals while repairing preservation regressions before any larger run.

## Generic changes only

- Retain download-set pressure against unverified guessed numbered PDF templates.
- Retain Writer post-save verification/DONE guard.
- Add a VS Code preservation guard: after current-user config JSON is written/validated, reject GUI drift that reopens and manually edits the same JSON.
- Add exact-color preservation guard: reject green-specific LibreOffice color values such as `#00A933` unless the task explicitly asks for green.

## Validation set

`configs/osworld_harness_v3_17_preserve_repair_5.json` includes download, Writer, VS Code, Impress, and one Calc sentinel.

## Gate

Run no 30-task set unless download and Writer stay improved while VS Code and Impress both return to 1. Calc is a sentinel only; do not require Calc recovery for this micro-run, but do not claim Calc progress unless it scores.
