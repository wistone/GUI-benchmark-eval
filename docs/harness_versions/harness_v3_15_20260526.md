# Harness V3.15 - failure-focus stabilization

Date: 2026-05-26

## Why

V3.14 tactical 30 completed at 16.9863/30, up about 2 points from the V3.4 tactical 30 baseline, but it did not satisfy the scale-up gate. The aggregate lift came mainly from unrelated late-run domains while the intended preservation/fix targets stayed unstable:

- Download-set preservation failed on the opened-folder CS50 lecture-slides task.
- Writer paragraph/tab-stop tasks scored 1/3 in tactical validation.
- Calc chart/sparkline tasks stayed weak.
- The first VS Code keybinding task consumed the full step budget and scored 0.

## Changes

- Keep V3.14 opened-folder destination guard and Office DONE gate.
- Strengthen generic repeated-download guidance: enumerate exact PDF hrefs before downloading; do not guess numbered URL templates until the hrefs have been verified.
- Add VS Code config guard: reject hard-coded `/home/oai` or `/home/user` VS Code config paths and require `~/.config/Code/User/...` or `$HOME/.config/Code/User/...`.
- Add focused warnings for:
  - Writer paragraph/line-spacing/tab-stop tasks.
  - Calc chart/sparkline/source-range tasks.
  - VS Code shortcut/settings tasks.

## Validation Plan

Run `configs/osworld_harness_v3_15_failure_focus_10.json`.

Promotion gate:

- V3.15 10-task failure-focus should recover at least several V3.14 failures, especially download or Writer/Calc, while preserving Impress exact-color, Chrome DNT, and VLC setting wins.
- If V3.15 remains near V3.14 on failures or regresses preservation items, do not run 30/100. Iterate again in 5-10 tasks.
- If V3.15 clearly improves failure clusters and preservation holds, rerun the 30-task tactical set before any 100-task stratified run.
