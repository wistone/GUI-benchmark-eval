# Harness V3.19

Date: 2026-05-26

## Goal

V3.19 is a repair-focused follow-up to V3.18. V3.18 improved the 30-task tactical score to 19.9863/30, but the gain came mostly from tail-domain preservation while the intended failure clusters stayed weak.

## Kept From V3.18

- Download target-folder and verified-template route.
- VS Code current-user config route.

## Generic Changes

- Prefer one quoted temp script plus one execution for artifact generation instead of line-by-line terminal entry.
- For generated artifacts, save to the requested/current/Desktop location unless the task explicitly asks for a subfolder.
- Add Calc guidance for exact fill-down, chart artifact verification, and native-sparkline infeasibility.
- Add a late Calc sparkline runner guard: after enough evidence/probing, reject continued non-FAIL attempts and require an evidence-backed FAIL rather than chart/shape emulation.
- Add Writer DOCX file-structure guidance for exact double spacing using `w:spacing` line `480` and `auto`.
- Add Impress exact named-color guidance and allow file-structure PPTX verification for `srgbClr` values.
- Add repetitive PDF/form guidance: inspect existing spreadsheet/template once, preserve layout, and generate exact requested filenames in the requested location.

## Validation

Run `osworld_harness_v3_19_repair_focus_8`:

- Calc x3
- Impress exact color
- Writer line spacing
- Multi-app PDF generation
- Download preservation
- VS Code preservation

Promotion gate: repair at least two failed clusters while preserving download and VS Code, then return to a 20-30 task gate. Do not jump directly to 100 or full.
